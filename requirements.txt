# ── Core ──────────────────────────────────────────────────────────────────────
honcho==1.1.0                 # Procfile-based process manager (make start-*)
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.0
structlog==24.2.0

# ── Async / Networking ────────────────────────────────────────────────────────
aiohttp==3.9.5
websockets==12.0
httpx==0.27.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0          # async PostgreSQL driver
alembic==1.13.1
redis==5.0.4             # includes async support

# ── Broker SDKs ───────────────────────────────────────────────────────────────
kiteconnect==5.0.1        # Zerodha Kite Connect

# ── Browser Automation (Zerodha daily re-auth) ───────────────────────────────
playwright==1.44.0
pyotp==2.9.0              # TOTP generation for 2FA

# ── Technical Analysis ────────────────────────────────────────────────────────
pandas>=2.2.2
numpy>=2.2.6
pandas-ta                 # 130+ indicators, pure Python
# TA-Lib requires system library: brew install ta-lib  →  then: pip install TA-Lib
# TA-Lib==0.4.28

# ── Data & Markets ────────────────────────────────────────────────────────────
yfinance==1.2.1           # fallback OHLCV data (no API key needed)
nsepython==2.9            # NSE India: option chains, FII/DII, indices

# ── AI ────────────────────────────────────────────────────────────────────────
anthropic==0.28.0

# ── Notifications ─────────────────────────────────────────────────────────────
python-telegram-bot==21.3

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler==3.10.4

# ── API Server ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── Utilities ─────────────────────────────────────────────────────────────────
pytz==2024.1
python-dateutil==2.9.0
tenacity==8.3.0           # retry logic for API calls
rich==13.7.1              # pretty terminal output
